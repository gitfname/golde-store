
export enum ENationalCardStatus {
    Pending = "pending",
    Rejected = "rejected",
    Accepted = "accepted"
}

export enum ENationalCardRejectionReason {
    NotMatching = "not-matching",
    LowQualityImage = "low-quality-image",
    Other = "other"
}