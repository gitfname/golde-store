
export enum ETransactionStatus {
    Pending = "pending",
    Accepted = "accepted",
    Rejected = "rejected"
}

export enum ETransactionType {
    ChargeWallet = "charge-wallet",
    RialToGold = "rial-to-gold",
    GoldToRial = "gold-to-rial"
}